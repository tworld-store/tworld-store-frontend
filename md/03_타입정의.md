# 03. 타입 정의 (TypeScript)

> **버전**: v1.0  
> **작성일**: 2025-11-06  
> **중요**: 이 파일의 모든 타입을 정확히 따를 것

---

## 📌 사용 방법

```typescript
// Admin 및 Functions에서 import
import type { Page, Section, Block, Product } from './types';

// 절대 수정하지 말 것
// 새 타입이 필요하면 명세서 업데이트 후 추가
```

---

## 🎯 핵심 타입

### Page (페이지)

```typescript
/**
 * 페이지 데이터 구조
 * Firestore 'pages' 컬렉션에 저장
 */
export interface Page {
  /** 고유 ID (Firestore 문서 ID) */
  id: string;
  
  /** 페이지 제목 */
  title: string;
  
  /** URL 슬러그 (예: "main", "about") */
  slug: string;
  
  /** 페이지 타입 */
  type: PageType;
  
  /** 섹션 배열 */
  sections: Section[];
  
  /** 메타 정보 (SEO) */
  meta: MetaData;
  
  /** 상태 */
  status: 'draft' | 'published';
  
  /** 버전 번호 */
  version: number;
  
  /** 생성일 */
  createdAt: Date;
  
  /** 수정일 */
  updatedAt: Date;
  
  /** 발행일 (published 상태일 때만) */
  publishedAt?: Date;
}

export type PageType = 
  | 'main'           // 메인 페이지
  | 'product-list'   // 상품 목록
  | 'product-detail' // 상품 상세
  | 'board'          // 게시판
  | 'custom';        // 커스텀 페이지

export interface MetaData {
  /** 메타 타이틀 */
  title: string;
  
  /** 메타 설명 */
  description: string;
  
  /** 메타 키워드 */
  keywords: string[];
  
  /** OG 이미지 */
  ogImage?: string;
}
```

---

### Section (섹션)

```typescript
/**
 * 섹션 데이터 구조
 * Page의 sections 배열에 포함
 */
export interface Section {
  /** 고유 ID */
  id: string;
  
  /** 섹션 타입 */
  type: SectionType;
  
  /** 섹션 이름 (사용자 지정) */
  name?: string;
  
  /** 표시 순서 */
  order: number;
  
  /** 섹션 설정 */
  settings: Record<string, any>;
  
  /** 반응형 설정 */
  responsive?: {
    desktop: SectionSettings;
    tablet?: SectionSettings;
    mobile?: SectionSettings;
  };
  
  /** 표시 여부 */
  visible: boolean;
  
  /** 스케줄링 설정 */
  schedule?: SectionSchedule;
}

export type SectionType =
  // 레이아웃
  | 'container'
  | 'grid'
  | 'flex'
  | 'section-wrapper'
  // 콘텐츠
  | 'hero-slider'
  | 'product-grid'
  | 'promotion-banner'
  | 'numbered-card-list'
  | 'numbered-product-grid'
  | 'text-section'
  | 'image-section'
  // 기능
  | 'consultation-form'
  | 'contact-info'
  | 'board-list'
  // 커스텀
  | 'custom';

export interface SectionSettings {
  /** 배경색 */
  background?: string;
  
  /** 패딩 */
  padding?: string;
  
  /** 여백 */
  margin?: string;
  
  /** 기타 설정 (섹션 타입별 다름) */
  [key: string]: any;
}

export interface SectionSchedule {
  /** 시작일 */
  startDate?: Date;
  
  /** 종료일 */
  endDate?: Date;
  
  /** 대상 디바이스 */
  targetDevices?: ('desktop' | 'tablet' | 'mobile')[];
}
```

---

### Block (블록)

```typescript
/**
 * 블록 데이터 구조
 * Section의 children으로 포함되거나 독립적으로 사용
 */
export interface Block {
  /** 고유 ID */
  id: string;
  
  /** 블록 타입 */
  type: BlockType;
  
  /** 블록 설정 */
  settings: BlockSettings;
  
  /** 자식 블록 (레이아웃 블록의 경우) */
  children?: Block[];
}

export type BlockType =
  // 기본
  | 'text'
  | 'image'
  | 'button'
  // 카드
  | 'product-card'
  | 'promo-card'
  | 'info-card'
  | 'post-card'
  // 폼
  | 'form'
  | 'input'
  | 'textarea'
  | 'select'
  // 특수
  | 'price-calculator'
  | 'badge';

export interface BlockSettings {
  /** 공통 설정 */
  className?: string;
  style?: React.CSSProperties;
  
  /** 블록 타입별 설정 */
  [key: string]: any;
}

// Text Block
export interface TextBlockSettings extends BlockSettings {
  content: string;
  size: 'xs' | 'sm' | 'base' | 'lg' | 'xl' | '2xl' | '3xl';
  weight: 'normal' | 'medium' | 'semibold' | 'bold';
  color?: string;
  align: 'left' | 'center' | 'right';
}

// Image Block
export interface ImageBlockSettings extends BlockSettings {
  src: string;
  alt: string;
  aspectRatio?: string;
  lazy: boolean;
  link?: string;
}

// Button Block
export interface ButtonBlockSettings extends BlockSettings {
  text: string;
  link: string;
  variant: 'primary' | 'secondary' | 'outline' | 'text';
  size: 'sm' | 'md' | 'lg';
  icon?: string;
  fullWidth: boolean;
}

// Badge Block
export interface BadgeBlockSettings extends BlockSettings {
  text: 'HOT' | 'NEW' | 'SALE' | string;
  color: string;
  position: 'top-left' | 'top-right' | 'bottom-left' | 'bottom-right';
}
```

---

### Product (상품)

```typescript
/**
 * 상품 데이터 구조
 * Google Sheets에서 동기화
 * Firestore 'products' 컬렉션에 저장
 */
export interface Product {
  /** 상품 ID (예: "갤럭시S24_256GB") */
  id: string;
  
  /** 브랜드 */
  brand: 'samsung' | 'apple' | 'etc';
  
  /** 모델명 */
  model: string;
  
  /** 용량 (GB) */
  storage: number;
  
  /** 출고가 */
  price: number;
  
  /** 색상 목록 */
  colors: ProductColor[];
  
  /** 표시 설정 */
  displaySettings: ProductDisplaySettings;
  
  /** 이미지 */
  images: ProductImages;
  
  /** 재고 상태 */
  stock: 'in-stock' | 'out-of-stock' | 'pre-order';
  
  /** 뱃지 */
  badges: ProductBadge[];
  
  /** 동기화 시간 */
  syncedAt: Date;
}

export interface ProductColor {
  /** 색상 코드 */
  code: string;
  
  /** 색상명 */
  name: string;
  
  /** HEX 값 */
  hex: string;
}

export interface ProductDisplaySettings {
  /** 상품 목록에 표시 */
  showInList: boolean;
  
  /** 추천 상품 */
  featured: boolean;
  
  /** 정렬 순서 */
  sortOrder: number;
}

export interface ProductImages {
  /** 메인 이미지 (색상별) */
  main: Record<string, string>; // { "black": "url", "white": "url" }
  
  /** 썸네일 (색상별) */
  thumbnails: Record<string, string[]>; // { "black": ["url1", "url2"] }
}

export interface ProductBadge {
  /** 뱃지 타입 */
  type: 'HOT' | 'NEW' | 'SALE' | 'SOLDOUT';
  
  /** 표시 여부 */
  visible: boolean;
  
  /** 시작일 */
  startDate?: Date;
  
  /** 종료일 */
  endDate?: Date;
}
```

---

### Plan (요금제)

```typescript
/**
 * 요금제 데이터 구조
 */
export interface Plan {
  /** 요금제 ID */
  id: string;
  
  /** 요금제명 */
  name: string;
  
  /** 월 기본료 */
  monthlyFee: number;
  
  /** 데이터 (GB) */
  data: number | '무제한';
  
  /** 음성 (분) */
  voice: number | '무제한';
  
  /** 문자 (건) */
  sms: number | '무제한';
  
  /** 통신사 */
  carrier: 'skt' | 'kt' | 'lgu+';
  
  /** 약정 할인율 */
  discountRate: number; // 0.25 = 25%
}
```

---

### PriceCalculation (가격 계산)

```typescript
/**
 * 가격 계산 파라미터
 */
export interface PriceCalculationParams {
  /** 상품 ID */
  deviceId: string;
  
  /** 요금제 ID */
  planId: string;
  
  /** 가입 유형 */
  subscriptionType: '기기변경' | '번호이동' | '신규가입';
  
  /** 할부 개월 */
  installmentMonths: 12 | 24 | 36 | 0; // 0 = 일시불
  
  /** 약정 유형 */
  contractType: '선약할인' | '공시지원';
  
  /** 인터넷+TV 결합 */
  hasCombination: boolean;
}

/**
 * 가격 계산 결과
 */
export interface PriceCalculationResult {
  /** 월 휴대폰 요금 */
  monthlyDeviceFee: {
    /** 출고가 */
    retailPrice: number;
    
    /** 휴대폰 할인금 */
    deviceDiscount: number;
    
    /** 추가 공시지원금 */
    additionalSubsidy: number;
    
    /** 할부원금 */
    installmentPrincipal: number;
  };
  
  /** 월 통신요금 */
  monthlyPlanFee: {
    /** 요금제 월 기본료 */
    baseFee: number;
    
    /** 약정할인 (지원금) */
    contractDiscount: number;
  };
  
  /** 예상 월 납부 금액 (부가세 포함) */
  estimatedMonthlyTotal: number;
}
```

---

### Consultation (상담)

```typescript
/**
 * 상담 신청 데이터 구조
 * Firestore 'consultations' 컬렉션에 저장
 */
export interface Consultation {
  /** 상담 ID */
  id: string;
  
  /** 고객명 */
  customerName: string;
  
  /** 전화번호 */
  phone: string;
  
  /** 상품 ID */
  deviceId: string;
  
  /** 요금제 ID */
  planId: string;
  
  /** 가입 유형 */
  subscriptionType: '기기변경' | '번호이동' | '신규가입';
  
  /** 추가 메시지 */
  message?: string;
  
  /** 상담 상태 */
  status: 'new' | 'processing' | 'completed' | 'cancelled';
  
  /** 알림 전송 여부 */
  notificationSent: {
    telegram: boolean;
    sms?: boolean;
    kakao?: boolean;
  };
  
  /** 생성일 */
  createdAt: Date;
  
  /** 처리일 */
  processedAt?: Date;
}

/**
 * 상담 신청 폼 데이터
 */
export interface ConsultationFormData {
  customerName: string;
  phone: string;
  deviceId: string;
  planId: string;
  subscriptionType: '기기변경' | '번호이동' | '신규가입';
  message?: string;
  agreeToPrivacy: boolean; // 필수 동의
}
```

---

### Board (게시판)

```typescript
/**
 * 게시판 데이터 구조
 * Firestore 'boards' 컬렉션에 저장
 */
export interface Board {
  /** 게시판 ID */
  id: string;
  
  /** 게시판명 */
  name: string;
  
  /** URL 슬러그 */
  slug: string;
  
  /** 노출 여부 */
  visible: boolean;
  
  /** 표시 순서 */
  order: number;
  
  /** 게시판 설정 */
  settings: BoardSettings;
  
  /** 생성일 */
  createdAt: Date;
  
  /** 수정일 */
  updatedAt: Date;
}

export interface BoardSettings {
  /** 댓글 허용 */
  allowComments: boolean;
  
  /** 첨부파일 허용 */
  allowAttachments: boolean;
  
  /** 최대 첨부파일 크기 (MB) */
  maxAttachmentSize: number;
  
  /** 페이지당 게시글 수 */
  postsPerPage: number;
}

/**
 * 게시글 데이터 구조
 * Firestore 'board_posts' 컬렉션에 저장
 */
export interface BoardPost {
  /** 게시글 ID */
  id: string;
  
  /** 게시판 ID */
  boardId: string;
  
  /** 제목 */
  title: string;
  
  /** 내용 (HTML) */
  content: string;
  
  /** 작성자 */
  author: string;
  
  /** 첨부파일 */
  attachments: Attachment[];
  
  /** 조회수 */
  views: number;
  
  /** 고정글 */
  pinned: boolean;
  
  /** 생성일 */
  createdAt: Date;
  
  /** 수정일 */
  updatedAt: Date;
}

export interface Attachment {
  /** 파일 타입 */
  type: 'image' | 'pdf' | 'doc' | 'etc';
  
  /** 파일명 */
  name: string;
  
  /** URL */
  url: string;
  
  /** 파일 크기 (bytes) */
  size: number;
}
```

---

### Menu (메뉴)

```typescript
/**
 * 메뉴 데이터 구조
 * Firestore 'menus' 컬렉션에 저장
 */
export interface Menu {
  /** 메뉴 ID */
  id: string;
  
  /** 메뉴명 */
  name: string;
  
  /** 링크 */
  link: string;
  
  /** 링크 타입 */
  linkType: 'internal' | 'external';
  
  /** 부모 메뉴 ID (2단계 메뉴) */
  parentId?: string;
  
  /** 표시 순서 */
  order: number;
  
  /** 노출 여부 */
  visible: boolean;
  
  /** 새 창 열기 */
  openNewTab: boolean;
}
```

---

## 🔄 API 타입

### API Response

```typescript
/**
 * 표준 API 응답 구조
 */
export interface ApiResponse<T = any> {
  /** 성공 여부 */
  success: boolean;
  
  /** 데이터 */
  data?: T;
  
  /** 에러 메시지 */
  error?: string;
  
  /** 에러 코드 */
  code?: string;
  
  /** 타임스탬프 */
  timestamp: string;
}

/**
 * 페이지네이션 응답
 */
export interface PaginatedResponse<T> {
  /** 데이터 배열 */
  items: T[];
  
  /** 전체 개수 */
  total: number;
  
  /** 현재 페이지 */
  page: number;
  
  /** 페이지당 개수 */
  perPage: number;
  
  /** 총 페이지 수 */
  totalPages: number;
  
  /** 다음 페이지 존재 여부 */
  hasNext: boolean;
  
  /** 이전 페이지 존재 여부 */
  hasPrev: boolean;
}
```

---

### Firebase Functions Request/Response

```typescript
/**
 * GET /api/products/list
 */
export interface GetProductsListRequest {
  brand?: 'samsung' | 'apple' | 'etc';
  minPrice?: number;
  maxPrice?: number;
  storage?: number[];
  sort?: 'price-asc' | 'price-desc' | 'latest' | 'popular';
  page?: number;
  perPage?: number;
}

export type GetProductsListResponse = ApiResponse<PaginatedResponse<Product>>;

/**
 * GET /api/products/:id
 */
export type GetProductDetailResponse = ApiResponse<Product>;

/**
 * POST /api/pages/generate
 */
export interface GeneratePageRequest {
  pageId: string;
}

export interface GeneratePageResponse {
  success: boolean;
  htmlUrl: string;
  deployUrl?: string;
}

/**
 * POST /api/forms/consultation
 */
export type SubmitConsultationRequest = ConsultationFormData;

export interface SubmitConsultationResponse {
  success: boolean;
  consultationId: string;
  message: string;
}

/**
 * POST /api/images/upload
 */
export interface UploadImageRequest {
  file: File | Blob;
  folder?: string;
}

export interface UploadImageResponse {
  success: boolean;
  url: string;
  publicId: string;
}
```

---

## 🎨 React Component Props

### PageBuilder Components

```typescript
/**
 * Editor.tsx
 */
export interface EditorProps {
  pageId: string;
  onSave?: (page: Page) => void;
  onPublish?: (page: Page) => void;
}

/**
 * Toolbar.tsx
 */
export interface ToolbarProps {
  onSave: () => void;
  onPublish: () => void;
  onPreview: () => void;
  isSaving: boolean;
}

/**
 * Sidebar.tsx
 */
export interface SidebarProps {
  sections: SectionType[];
  onAddSection: (type: SectionType) => void;
}

/**
 * Canvas.tsx
 */
export interface CanvasProps {
  page: Page;
  mode: 'edit' | 'preview';
}
```

### Block Components

```typescript
/**
 * TextBlock.tsx
 */
export interface TextBlockProps {
  content: string;
  size?: TextBlockSettings['size'];
  weight?: TextBlockSettings['weight'];
  color?: string;
  align?: TextBlockSettings['align'];
}

/**
 * ImageBlock.tsx
 */
export interface ImageBlockProps {
  src: string;
  alt: string;
  aspectRatio?: string;
  lazy?: boolean;
  link?: string;
}

/**
 * ButtonBlock.tsx
 */
export interface ButtonBlockProps {
  text: string;
  link: string;
  variant?: ButtonBlockSettings['variant'];
  size?: ButtonBlockSettings['size'];
  icon?: string;
  fullWidth?: boolean;
  onClick?: () => void;
}

/**
 * ProductCard.tsx
 */
export interface ProductCardProps {
  product: Product;
  style: 'simple' | 'detailed';
  showBadge?: boolean;
  onCardClick?: (productId: string) => void;
}
```

---

## 🛠️ Utility Types

```typescript
/**
 * Firestore Timestamp → Date 변환
 */
export type FirestoreTimestamp = {
  seconds: number;
  nanoseconds: number;
};

export type WithFirestoreDate<T> = Omit<T, 'createdAt' | 'updatedAt' | 'publishedAt'> & {
  createdAt: FirestoreTimestamp;
  updatedAt: FirestoreTimestamp;
  publishedAt?: FirestoreTimestamp;
};

/**
 * Partial Update (일부 필드만 업데이트)
 */
export type PartialUpdate<T> = {
  [P in keyof T]?: T[P];
};

/**
 * Required Fields (특정 필드 필수)
 */
export type RequireFields<T, K extends keyof T> = T & Required<Pick<T, K>>;

/**
 * Exclude Fields (특정 필드 제외)
 */
export type ExcludeFields<T, K extends keyof T> = Omit<T, K>;
```

---

## 📚 다음 문서

- [API 명세 (요청/응답 상세)](./04_API명세.md)
- [디자인 시스템 (CSS 변수 전체)](./05_디자인시스템.md)

---

**문서 버전**: v1.0  
**최종 업데이트**: 2025-11-06  
**중요**: 이 타입을 절대 임의로 수정하지 말 것
