# 09. 개발 가이드라인

> **버전**: v1.0  
> **작성일**: 2025-11-06  
> **목적**: 프로젝트 전체의 코드 일관성 유지  
> **적용 범위**: Admin, Frontend, Functions, Shared 모듈

---

## 📌 개요

### 이 문서의 목적
- 모든 개발자가 일관된 코드 스타일 유지
- 코드 리뷰 시 명확한 기준 제공
- 유지보수성 향상
- 버그 발생 가능성 감소

### 준수 원칙
```yaml
우선순위:
  1. 가독성 > 간결성
  2. 명확성 > 영리함
  3. 유지보수성 > 성능 (단, 심각한 성능 이슈 제외)
  4. 명세서 준수 > 개인 취향
```

---

## 🏷️ 네이밍 컨벤션

### 1. 변수명

#### **일반 변수**
```yaml
규칙: camelCase
설명: 명사 또는 명사구
길이: 의미 전달에 충분한 길이
```

**✅ 좋은 예시:**
```typescript
// 명확하고 의미 전달
const userName = 'John';
const totalPrice = 150000;
const isAuthenticated = true;
const productList = [];
const selectedColor = 'black';

// 배열은 복수형
const products = [];
const images = [];
const categories = [];

// 불린은 is/has/can으로 시작
const isVisible = true;
const hasError = false;
const canEdit = true;
```

**❌ 나쁜 예시:**
```typescript
// 너무 짧거나 의미 불명확
const n = 'John';
const p = 150000;
const flag = true;
const arr = [];

// 헝가리안 표기법 사용
const strName = 'John';
const intPrice = 150000;

// 약어 남용
const usrNm = 'John';
const prdLst = [];
```

#### **상수**
```yaml
규칙: UPPER_SNAKE_CASE
설명: 전역 상수, 환경 변수, 설정값
위치: 파일 최상단 또는 별도 constants 파일
```

**✅ 좋은 예시:**
```typescript
// 환경 변수
const API_BASE_URL = 'https://api.example.com';
const MAX_FILE_SIZE = 5 * 1024 * 1024; // 5MB
const DEFAULT_PAGE_SIZE = 10;

// 설정 상수
const SUPPORTED_IMAGE_TYPES = ['jpg', 'jpeg', 'png', 'webp'];
const CACHE_DURATION = 60 * 60 * 1000; // 1시간

// 에러 코드
const ERROR_CODE = {
  INVALID_INPUT: 'E001',
  UNAUTHORIZED: 'E002',
  NOT_FOUND: 'E003'
};
```

**❌ 나쁜 예시:**
```typescript
// camelCase 사용
const apiBaseUrl = 'https://api.example.com';

// 값이 변경 가능한 변수를 대문자로
let MAX_RETRY = 3; // let은 변경 가능하므로 부적절
```

---

### 2. 함수명

#### **일반 함수**
```yaml
규칙: camelCase
설명: 동사 또는 동사구로 시작
명명 패턴:
  - get/fetch: 데이터 조회
  - set/update: 데이터 수정
  - create/add: 데이터 생성
  - delete/remove: 데이터 삭제
  - calculate/compute: 계산
  - validate/check: 검증
  - handle: 이벤트 처리
```

**✅ 좋은 예시:**
```typescript
// 데이터 조회
function getUserById(id: string): User { }
function fetchProducts(): Promise<Product[]> { }

// 데이터 수정
function updateUserName(id: string, name: string): void { }
function setActiveTab(tabId: string): void { }

// 데이터 생성/삭제
function createOrder(data: OrderData): Order { }
function deleteProduct(id: string): Promise<void> { }

// 계산
function calculateTotalPrice(items: CartItem[]): number { }
function computeMonthlyPayment(price: number, months: number): number { }

// 검증
function validateEmail(email: string): boolean { }
function checkAuthentication(): boolean { }

// 이벤트 핸들러
function handleSubmit(event: FormEvent): void { }
function handleColorChange(color: string): void { }

// 유틸리티
function formatPrice(price: number): string { }
function parseQueryString(url: string): Record<string, string> { }
```

**❌ 나쁜 예시:**
```typescript
// 명사로 시작
function user(id: string) { } // getUserById로 변경

// 의미 불명확
function process(data: any) { } // 무엇을 처리하는지 불명확
function doIt() { } // 무엇을 하는지 불명확

// 너무 긴 이름
function getUserByIdAndReturnTheUserObjectWithAllRelatedData() { }

// 약어 남용
function chkAuth() { } // checkAuthentication으로 변경
```

#### **React 컴포넌트 함수**
```yaml
규칙: PascalCase
설명: 명사로 시작
패턴: [도메인][기능]Component 또는 [기능][도메인]
```

**✅ 좋은 예시:**
```typescript
// 일반 컴포넌트
function ProductCard() { }
function UserProfile() { }
function OrderList() { }

// 레이아웃 컴포넌트
function MainLayout() { }
function AdminLayout() { }

// 폼 컴포넌트
function LoginForm() { }
function ConsultationForm() { }

// 페이지 컴포넌트
function ProductDetailPage() { }
function HomePage() { }
```

**❌ 나쁜 예시:**
```typescript
// camelCase 사용
function productCard() { } // ProductCard로 변경

// 동사로 시작
function RenderProduct() { } // Product 또는 ProductView로 변경

// 너무 일반적
function Component1() { }
function Item() { }
```

---

### 3. 타입/인터페이스명

```yaml
규칙: PascalCase
설명: 명사 또는 명사구
접미사:
  - Props: React 컴포넌트 props
  - State: 상태 타입
  - Data: API 데이터
  - Config: 설정
  - Error: 에러
```

**✅ 좋은 예시:**
```typescript
// 일반 타입
interface User {
  id: string;
  name: string;
}

interface Product {
  id: string;
  name: string;
  price: number;
}

// Props 타입
interface ProductCardProps {
  product: Product;
  onClick?: () => void;
}

// State 타입
interface AppState {
  user: User | null;
  isLoading: boolean;
}

// API 데이터 타입
interface ProductListData {
  products: Product[];
  total: number;
  page: number;
}

// 설정 타입
interface FirebaseConfig {
  apiKey: string;
  projectId: string;
}

// 에러 타입
interface ValidationError {
  field: string;
  message: string;
}

// Enum 타입
enum OrderStatus {
  Pending = 'PENDING',
  Confirmed = 'CONFIRMED',
  Shipped = 'SHIPPED',
  Delivered = 'DELIVERED'
}
```

**❌ 나쁜 예시:**
```typescript
// camelCase 사용
interface user { } // User로 변경

// I 접두사 사용 (TypeScript에서 비권장)
interface IUser { } // User로 변경

// 너무 일반적
interface Data { }
interface Config { }

// 약어 남용
interface PrdData { } // ProductData로 변경
```

---

### 4. 파일명

#### **컴포넌트 파일**
```yaml
규칙: PascalCase.tsx
설명: 컴포넌트명과 동일
위치: /components, /pages
```

**✅ 좋은 예시:**
```
ProductCard.tsx
UserProfile.tsx
ConsultationForm.tsx
MainLayout.tsx
HomePage.tsx
```

**❌ 나쁜 예시:**
```
productCard.tsx     // camelCase 사용
product-card.tsx    // kebab-case 사용
Product_Card.tsx    // snake_case 혼용
```

#### **유틸리티/모듈 파일**
```yaml
규칙: camelCase.ts 또는 kebab-case.ts
설명: 기능을 명확히 표현
위치: /utils, /modules, /lib
```

**✅ 좋은 예시:**
```
calculator.ts
api.ts
validators.ts
url-builder.ts
date-formatter.ts
storage-helper.ts
```

**❌ 나쁜 예시:**
```
utils.ts           // 너무 일반적
helpers.ts         // 너무 일반적
calc.ts            // 약어 사용
```

#### **훅 파일**
```yaml
규칙: use[기능명].ts
설명: React 훅은 use로 시작
위치: /hooks
```

**✅ 좋은 예시:**
```
useAuth.ts
useProducts.ts
useLocalStorage.ts
useDebounce.ts
```

---

## 📝 코딩 규칙

### 1. 들여쓰기

```yaml
규칙: 스페이스 2칸
도구: Prettier 자동 포맷
설정: .prettierrc
```

**✅ 좋은 예시:**
```typescript
function calculatePrice(items: CartItem[]): number {
  let total = 0;
  
  for (const item of items) {
    if (item.quantity > 0) {
      total += item.price * item.quantity;
    }
  }
  
  return total;
}
```

**❌ 나쁜 예시:**
```typescript
// 탭 사용
function calculatePrice(items: CartItem[]): number {
	let total = 0;
	return total;
}

// 스페이스 4칸
function calculatePrice(items: CartItem[]): number {
    let total = 0;
    return total;
}
```

---

### 2. 세미콜론

```yaml
규칙: 항상 사용
이유: ASI(Automatic Semicolon Insertion) 오류 방지
```

**✅ 좋은 예시:**
```typescript
const name = 'John';
const age = 30;

function greet() {
  console.log('Hello');
}

export default App;
```

**❌ 나쁜 예시:**
```typescript
const name = 'John'
const age = 30

function greet() {
  console.log('Hello')
}
```

---

### 3. 따옴표

```yaml
규칙: 작은따옴표 (')
예외: JSX에서는 큰따옴표 (")
이유: 일관성 유지
```

**✅ 좋은 예시:**
```typescript
// TypeScript
const message = 'Hello World';
const path = '/api/products';

// JSX
<div className="container">
  <span data-testid="username">John</span>
</div>
```

**❌ 나쁜 예시:**
```typescript
// TypeScript에서 큰따옴표
const message = "Hello World";

// JSX에서 작은따옴표
<div className='container'></div>
```

---

### 4. 화살표 함수

```yaml
규칙: 
  - 짧은 함수: 화살표 함수 권장
  - 긴 함수: function 키워드 사용
  - 메서드: function 키워드 사용
이유: 가독성 및 this 바인딩 명확성
```

**✅ 좋은 예시:**
```typescript
// 짧은 함수 - 화살표 함수
const add = (a: number, b: number): number => a + b;
const double = (n: number): number => n * 2;

// 배열 메서드 - 화살표 함수
const prices = products.map(p => p.price);
const total = prices.reduce((sum, price) => sum + price, 0);

// 긴 함수 - function 키워드
function calculateMonthlyPayment(
  price: number,
  months: number,
  interestRate: number
): number {
  // 복잡한 계산 로직
  const monthlyRate = interestRate / 12 / 100;
  const payment = price * monthlyRate / (1 - Math.pow(1 + monthlyRate, -months));
  
  return Math.round(payment);
}

// React 컴포넌트 - function 키워드
function ProductCard({ product }: ProductCardProps) {
  // 여러 훅과 로직
  const [isHovered, setIsHovered] = useState(false);
  
  return (
    <div>...</div>
  );
}
```

**❌ 나쁜 예시:**
```typescript
// 긴 함수를 화살표로
const calculateMonthlyPayment = (price: number, months: number): number => {
  // 50줄 이상의 로직...
  return result;
};
```

---

### 5. 비동기 처리

```yaml
규칙: async/await 사용 (Promise.then 지양)
이유: 가독성 및 에러 처리 명확성
예외: Promise.all 등 병렬 처리
```

**✅ 좋은 예시:**
```typescript
// async/await
async function fetchProductDetail(id: string): Promise<Product> {
  try {
    const response = await fetch(`/api/products/${id}`);
    
    if (!response.ok) {
      throw new Error(`HTTP error! status: ${response.status}`);
    }
    
    const data = await response.json();
    return data;
  } catch (error) {
    console.error('Failed to fetch product:', error);
    throw error;
  }
}

// 병렬 처리
async function fetchAllData(): Promise<void> {
  try {
    const [products, categories, brands] = await Promise.all([
      fetchProducts(),
      fetchCategories(),
      fetchBrands()
    ]);
    
    // 데이터 처리
  } catch (error) {
    console.error('Failed to fetch data:', error);
  }
}
```

**❌ 나쁜 예시:**
```typescript
// Promise.then 체인
function fetchProductDetail(id: string): Promise<Product> {
  return fetch(`/api/products/${id}`)
    .then(response => {
      if (!response.ok) {
        throw new Error('Failed');
      }
      return response.json();
    })
    .then(data => {
      return data;
    })
    .catch(error => {
      console.error(error);
      throw error;
    });
}
```

---

### 6. 조건문

```yaml
규칙:
  - 긍정 조건 우선
  - 중괄호 항상 사용
  - Early return 패턴 권장
```

**✅ 좋은 예시:**
```typescript
// 긍정 조건
if (isAuthenticated) {
  showDashboard();
} else {
  showLogin();
}

// Early return
function processOrder(order: Order): void {
  if (!order) {
    console.error('Order is null');
    return;
  }
  
  if (order.status !== 'PENDING') {
    console.error('Order is not pending');
    return;
  }
  
  // 메인 로직
  completeOrder(order);
}

// 중괄호 사용
if (condition) {
  doSomething();
}
```

**❌ 나쁜 예시:**
```typescript
// 부정 조건
if (!isNotAuthenticated) { } // 이중 부정

// 중괄호 생략
if (condition) doSomething();

// 깊은 중첩
function processOrder(order: Order): void {
  if (order) {
    if (order.status === 'PENDING') {
      if (order.items.length > 0) {
        // 메인 로직
      }
    }
  }
}
```

---

### 7. 변수 선언

```yaml
규칙:
  - const 우선 사용
  - let은 재할당 필요 시만
  - var 절대 사용 금지
```

**✅ 좋은 예시:**
```typescript
// const 사용
const userName = 'John';
const products = [];

// let은 재할당 필요 시만
let total = 0;
for (const item of items) {
  total += item.price;
}

// 블록 스코프
function example() {
  const x = 10;
  
  if (true) {
    const y = 20; // 블록 내에서만 유효
  }
}
```

**❌ 나쁜 예시:**
```typescript
// var 사용
var userName = 'John';

// 불필요한 let 사용
let userName = 'John'; // 재할당하지 않는데 let 사용
```

---

## 💬 주석 작성 규칙

### 1. 파일 헤더 주석

```yaml
규칙: 모든 주요 파일 상단에 작성
내용: 파일 목적, 주요 기능, 작성자(선택)
형식: JSDoc 스타일
```

**✅ 좋은 예시:**
```typescript
/**
 * 상품 가격 계산기
 * 
 * 상품의 최종 가격을 계산하는 핵심 모듈입니다.
 * - 출고가 계산
 * - 할부 금액 계산
 * - 월 납부액 계산
 * - 공시지원금 적용
 * 
 * @module calculator
 * @version 1.0.0
 */

// import 문...
```

---

### 2. 함수 주석 (JSDoc)

```yaml
규칙: 
  - 모든 export 함수에 필수
  - private 함수는 복잡한 경우만
  - 파라미터, 반환값, 예외 설명
언어: 한국어
```

**✅ 좋은 예시:**
```typescript
/**
 * 월 할부금을 계산합니다.
 * 
 * 출고가를 기준으로 선택한 할부 개월 수에 따라
 * 매월 납부해야 할 금액을 계산합니다.
 * 
 * @param price - 기기 출고가 (원)
 * @param months - 할부 개월 수 (12, 24, 36)
 * @param downPayment - 선납금 (원, 기본값 0)
 * @returns 월 할부금 (원, 소수점 이하 반올림)
 * @throws {Error} 할부 개월이 유효하지 않을 때
 * 
 * @example
 * ```typescript
 * const monthly = calculateInstallment(1200000, 24);
 * // 50000
 * ```
 */
function calculateInstallment(
  price: number,
  months: number,
  downPayment: number = 0
): number {
  if (![12, 24, 36].includes(months)) {
    throw new Error('유효하지 않은 할부 개월입니다.');
  }
  
  const remainingPrice = price - downPayment;
  return Math.round(remainingPrice / months);
}
```

**❌ 나쁜 예시:**
```typescript
// 주석 없음
function calculateInstallment(price: number, months: number): number {
  return Math.round(price / months);
}

// 의미 없는 주석
/**
 * 계산한다
 */
function calculateInstallment(price: number, months: number): number {
  return Math.round(price / months);
}

// 영어 주석
/**
 * Calculate installment
 * @param price price
 * @param months months
 */
function calculateInstallment(price: number, months: number): number {
  return Math.round(price / months);
}
```

---

### 3. 인라인 주석

```yaml
규칙:
  - 복잡한 로직에만 사용
  - Why를 설명 (What은 코드로)
  - 코드 위에 작성 (옆은 지양)
```

**✅ 좋은 예시:**
```typescript
function processPayment(amount: number): void {
  // 네이버페이는 100원 미만 결제 불가
  if (amount < 100) {
    throw new Error('최소 결제 금액은 100원입니다.');
  }
  
  // 부가세 10% 자동 포함
  const taxIncluded = Math.floor(amount * 1.1);
  
  // 원 단위 절사 (PG사 정책)
  const finalAmount = Math.floor(taxIncluded / 10) * 10;
  
  processTransaction(finalAmount);
}
```

**❌ 나쁜 예시:**
```typescript
function processPayment(amount: number): void {
  if (amount < 100) { // 100원 체크
    throw new Error('최소 결제 금액은 100원입니다.');
  }
  
  const taxIncluded = amount * 1.1; // 세금 계산
  const finalAmount = Math.floor(taxIncluded / 10) * 10; // 절사
  
  processTransaction(finalAmount); // 처리
}
```

---

### 4. TODO 주석

```yaml
규칙:
  - TODO: 구현 예정
  - FIXME: 버그 수정 필요
  - HACK: 임시 방편
  - NOTE: 중요 참고사항
형식: // TODO: [작성자] 설명
```

**✅ 좋은 예시:**
```typescript
// TODO: [2025-11-10] 카카오페이 연동 추가
function initPaymentMethods(): void {
  // 현재는 네이버페이만 지원
}

// FIXME: [김철수] Safari에서 이미지 로딩 느림
function loadProductImage(url: string): void {
  // ...
}

// HACK: Cloudinary API 제한 우회
// 정식 해결 전까지 딜레이 추가
async function uploadImage(file: File): Promise<string> {
  await delay(100);
  return upload(file);
}

// NOTE: 이 함수는 GAS에서도 동일한 로직 사용 중
// 수정 시 GAS 코드도 함께 업데이트 필요
function parseProductData(row: any[]): Product {
  // ...
}
```

---

## 🐛 에러 처리

### 1. Try-Catch

```yaml
규칙:
  - async 함수는 항상 try-catch
  - 에러 로깅 필수
  - 사용자에게 명확한 메시지
```

**✅ 좋은 예시:**
```typescript
/**
 * 상품 목록을 가져옵니다.
 */
async function fetchProducts(): Promise<Product[]> {
  try {
    const response = await fetch('/api/products');
    
    if (!response.ok) {
      throw new Error(`HTTP ${response.status}: ${response.statusText}`);
    }
    
    const data = await response.json();
    return data.products;
    
  } catch (error) {
    // 에러 로깅
    console.error('상품 목록 조회 실패:', error);
    
    // 에러 타입별 처리
    if (error instanceof TypeError) {
      throw new Error('네트워크 연결을 확인해주세요.');
    }
    
    // 기본 에러 메시지
    throw new Error('상품 목록을 불러올 수 없습니다.');
  }
}
```

**❌ 나쁜 예시:**
```typescript
// try-catch 없음
async function fetchProducts(): Promise<Product[]> {
  const response = await fetch('/api/products');
  return response.json();
}

// 에러를 무시
async function fetchProducts(): Promise<Product[]> {
  try {
    const response = await fetch('/api/products');
    return response.json();
  } catch (error) {
    // 아무것도 안 함
  }
}

// 의미 없는 에러 메시지
async function fetchProducts(): Promise<Product[]> {
  try {
    const response = await fetch('/api/products');
    return response.json();
  } catch (error) {
    throw new Error('에러 발생');
  }
}
```

---

### 2. 커스텀 에러

```yaml
규칙: 도메인별 에러 클래스 정의
위치: /shared/errors/
명명: [도메인]Error
```

**✅ 좋은 예시:**
```typescript
/**
 * API 관련 에러
 */
export class ApiError extends Error {
  constructor(
    message: string,
    public statusCode: number,
    public errorCode?: string
  ) {
    super(message);
    this.name = 'ApiError';
  }
}

/**
 * 유효성 검증 에러
 */
export class ValidationError extends Error {
  constructor(
    message: string,
    public field: string
  ) {
    super(message);
    this.name = 'ValidationError';
  }
}

// 사용
async function createOrder(data: OrderData): Promise<Order> {
  // 유효성 검증
  if (!data.userId) {
    throw new ValidationError('사용자 ID가 필요합니다.', 'userId');
  }
  
  // API 호출
  try {
    const response = await fetch('/api/orders', {
      method: 'POST',
      body: JSON.stringify(data)
    });
    
    if (!response.ok) {
      throw new ApiError(
        '주문 생성 실패',
        response.status,
        'ORDER_CREATE_FAILED'
      );
    }
    
    return response.json();
  } catch (error) {
    if (error instanceof ApiError) {
      console.error(`API Error [${error.errorCode}]:`, error.message);
    }
    throw error;
  }
}
```

---

### 3. 에러 바운더리 (React)

```yaml
규칙: 
  - 페이지 레벨에 필수
  - 사용자 친화적 폴백 UI
  - 에러 로깅
```

**✅ 좋은 예시:**
```typescript
/**
 * 에러 바운더리 컴포넌트
 */
class ErrorBoundary extends React.Component<
  { children: React.ReactNode },
  { hasError: boolean; error?: Error }
> {
  constructor(props: any) {
    super(props);
    this.state = { hasError: false };
  }

  static getDerivedStateFromError(error: Error) {
    return { hasError: true, error };
  }

  componentDidCatch(error: Error, errorInfo: React.ErrorInfo) {
    // 에러 로깅
    console.error('React Error Boundary:', error, errorInfo);
    
    // 외부 로깅 서비스에 전송 (선택)
    // logErrorToService(error, errorInfo);
  }

  render() {
    if (this.state.hasError) {
      return (
        <div className="error-fallback">
          <h1>죄송합니다. 오류가 발생했습니다.</h1>
          <p>페이지를 새로고침하거나 잠시 후 다시 시도해주세요.</p>
          <button onClick={() => window.location.reload()}>
            새로고침
          </button>
        </div>
      );
    }

    return this.props.children;
  }
}
```

---

## 📦 Git 커밋 메시지 규칙

### 1. 커밋 메시지 형식

```yaml
형식: <type>: <subject>

type:
  - feat: 새 기능
  - fix: 버그 수정
  - docs: 문서 수정
  - style: 코드 포맷팅 (기능 변경 없음)
  - refactor: 리팩토링
  - test: 테스트 추가/수정
  - chore: 빌드, 설정 변경

subject:
  - 명령문 (한국어)
  - 50자 이내
  - 마침표 없음
```

**✅ 좋은 예시:**
```bash
feat: 상품 필터 기능 추가
fix: 가격 계산 오류 수정
docs: README에 설치 가이드 추가
style: 코드 포맷팅 (Prettier)
refactor: 상품 카드 컴포넌트 분리
test: 계산기 모듈 테스트 추가
chore: Firebase 설정 업데이트
```

**❌ 나쁜 예시:**
```bash
# 타입 없음
상품 필터 추가

# 과거형
feat: 상품 필터를 추가했습니다

# 너무 장황
feat: 사용자가 상품을 색상, 가격, 브랜드별로 필터링할 수 있는 기능을 추가했습니다

# 의미 불명확
fix: 버그 수정
update: 수정
```

---

### 2. 커밋 단위

```yaml
규칙:
  - 하나의 논리적 변경사항 = 하나의 커밋
  - 너무 크거나 작지 않게
  - 빌드 가능한 상태로 커밋
```

**✅ 좋은 예시:**
```bash
# 적절한 단위
feat: 상품 검색 기능 추가
feat: 상품 검색 결과 하이라이팅 추가
feat: 상품 검색 자동완성 추가

# 관련 파일을 한 번에
feat: 로그인 폼 컴포넌트 추가
  - components/LoginForm.tsx
  - components/LoginForm.test.tsx
  - types/auth.ts
```

**❌ 나쁜 예시:**
```bash
# 너무 많은 변경사항
feat: 로그인, 회원가입, 비밀번호 찾기 추가

# 너무 작은 단위
feat: 변수명 변경
feat: 주석 추가
feat: 세미콜론 추가
```

---

### 3. 브랜치 전략

```yaml
브랜치:
  - main: 프로덕션 (배포)
  - develop: 개발
  - feature/기능명: 새 기능
  - fix/버그명: 버그 수정
  - hotfix/버그명: 긴급 수정

명명 규칙:
  - kebab-case
  - 간결하고 명확하게
```

**✅ 좋은 예시:**
```bash
# 기능 개발
feature/product-filter
feature/user-auth
feature/payment-integration

# 버그 수정
fix/price-calculation
fix/image-loading

# 긴급 수정
hotfix/security-patch
hotfix/critical-bug
```

**❌ 나쁜 예시:**
```bash
# 너무 일반적
feature/new-feature
fix/bug

# 너무 장황
feature/add-product-filter-by-color-and-price

# 개인 브랜치
feature/john-work
```

---

## 🧪 테스트 작성 가이드

### 1. 테스트 파일명

```yaml
규칙: [파일명].test.ts 또는 [파일명].spec.ts
위치: 원본 파일과 동일한 폴더
```

**✅ 좋은 예시:**
```
calculator.ts
calculator.test.ts

ProductCard.tsx
ProductCard.test.tsx
```

---

### 2. 테스트 구조

```yaml
규칙:
  - describe: 테스트 그룹
  - it/test: 개별 테스트
  - 한국어 설명
  - AAA 패턴 (Arrange, Act, Assert)
```

**✅ 좋은 예시:**
```typescript
import { calculateInstallment } from './calculator';

describe('calculateInstallment', () => {
  describe('정상 케이스', () => {
    it('12개월 할부 금액을 정확히 계산한다', () => {
      // Arrange (준비)
      const price = 1200000;
      const months = 12;
      
      // Act (실행)
      const result = calculateInstallment(price, months);
      
      // Assert (검증)
      expect(result).toBe(100000);
    });
    
    it('선납금을 제외하고 계산한다', () => {
      const price = 1200000;
      const months = 12;
      const downPayment = 200000;
      
      const result = calculateInstallment(price, months, downPayment);
      
      expect(result).toBe(83333); // (1200000 - 200000) / 12
    });
  });
  
  describe('예외 케이스', () => {
    it('유효하지 않은 할부 개월이면 에러를 던진다', () => {
      const price = 1200000;
      const months = 6; // 지원하지 않는 개월
      
      expect(() => {
        calculateInstallment(price, months);
      }).toThrow('유효하지 않은 할부 개월입니다.');
    });
  });
});
```

---

## 📚 참고 문서

- [타입 정의](./03_타입정의.md)
- [API 명세](./04_API명세.md)
- [핵심 모듈](./06_핵심모듈.md)
- [컴포넌트 명세](./07_컴포넌트명세.md)

---

## ⚠️ 중요 주의사항

### **반드시 지켜야 할 것**
1. ✅ 명세서에 정의된 타입과 함수 시그니처 준수
2. ✅ 모든 export 함수에 JSDoc 주석 (한국어)
3. ✅ async 함수는 try-catch로 에러 처리
4. ✅ const 우선, let은 필요 시만, var 금지
5. ✅ 커밋 메시지는 conventional commits 형식

### **권장사항**
1. 💡 복잡한 로직은 작은 함수로 분리
2. 💡 매직 넘버는 상수로 정의
3. 💡 중복 코드는 함수/유틸로 추출
4. 💡 Early return 패턴 활용
5. 💡 테스트 코드 작성 (주요 로직)

### **절대 금지**
1. ❌ 명세서에 없는 타입/함수 임의 추가
2. ❌ console.log 프로덕션 코드에 남기기
3. ❌ any 타입 남용
4. ❌ 의미 없는 주석
5. ❌ 하드코딩된 설정값 (환경변수 사용)

---

## 🔧 개발 도구 설정

### ESLint 설정 (.eslintrc.js)
```javascript
module.exports = {
  extends: [
    'eslint:recommended',
    'plugin:@typescript-eslint/recommended',
    'plugin:react/recommended',
    'plugin:react-hooks/recommended'
  ],
  rules: {
    'semi': ['error', 'always'],
    'quotes': ['error', 'single'],
    '@typescript-eslint/no-explicit-any': 'warn',
    'react/react-in-jsx-scope': 'off'
  }
};
```

### Prettier 설정 (.prettierrc)
```json
{
  "semi": true,
  "singleQuote": true,
  "tabWidth": 2,
  "trailingComma": "es5",
  "printWidth": 80,
  "arrowParens": "avoid"
}
```

### TypeScript 설정 (tsconfig.json)
```json
{
  "compilerOptions": {
    "target": "ES2020",
    "module": "ESNext",
    "lib": ["ES2020", "DOM"],
    "jsx": "react-jsx",
    "strict": true,
    "noUnusedLocals": true,
    "noUnusedParameters": true,
    "noImplicitReturns": true
  }
}
```

---

**문서 버전**: v1.0  
**최종 업데이트**: 2025-11-06  
**다음 리뷰**: Phase 2 시작 전
