📌 티월드스토어 웹빌더 프로젝트 - 작업 연속성 문서
🎯 프로젝트 현황 요약
확정 사항
yaml프로젝트명: tworld-store-builder
목적: 아임웹 → 자체 웹빌더 전환
기간: 8주 개발
예산: 월 2.5만원 운영비 (도메인 제외)
트래픽: 일 1000명 기준

✅ 확정된 기술 스택 및 구조
기술 결정사항
구분결정 내용이유AdminReact + 웹빌더페이지 구조 완전 제어데이터Google Sheets + GAS기존 운영 방식 유지이미지CloudinaryAI 편집, 자동 최적화호스팅Vercel + Firebase성능 + 기능 균형모듈ES6 ModulesAdmin 편의성URL?model=모델명SEO 최적화
핵심 기능 요구사항
markdown✅ 상담 시스템: 폼 → Google Sheets → 텔레그램 봇
✅ 게시판: Admin에서 자유롭게 생성/관리
✅ 디자인: 기존 HTML 기반 통일성
✅ 상품 관리: Sheets 자동 동기화
✅ 문자/카톡: 추후 구현 예정
```

---

## 📁 **프로젝트 구조 (최종)**
```
tworld-store-builder/
├── admin/           # 웹빌더 Admin (React)
├── frontend/        # 생성된 정적 사이트
├── functions/       # Firebase Functions
├── shared/          # 공유 모듈
│   ├── design-system/  # 기존 HTML 추출 디자인
│   └── modules/        # calculator.js, api.js
├── templates/       # 페이지/섹션 템플릿
└── migration/       # 아임웹 데이터 이전 도구

📊 지침서 위치
새 프로젝트에 업로드할 파일

프로젝트 완전 지침서 v1.0 (위에서 작성한 전체 지침서)
이 작업 연속성 문서


🚀 다음 작업 순서
즉시 시작할 작업
markdown1. [ ] Firebase 프로젝트 생성
2. [ ] 프로젝트 초기 구조 셋업
3. [ ] 기존 HTML에서 디자인 시스템 추출
4. [ ] Google Sheets 연동 테스트
5. [ ] 텔레그램 봇 코드 마이그레이션
Phase별 체크포인트
markdownWeek 1-2: 인프라 + 디자인 시스템
Week 3-4: Admin 페이지 빌더
Week 5-6: 이미지/콘텐츠 관리
Week 7-8: 통합 테스트 + 마이그레이션
```

---

## 💬 **다음 대화 시작 멘트**
```
"티월드스토어 웹빌더 프로젝트를 이어서 진행합니다.
현재 [작업 내용]을 시작하려고 합니다.
[구체적인 요청사항]을 만들어주세요."

📝 참고사항

기존 텔레그램 봇 코드: 있음 (수정 필요)
기존 정적 HTML: 상품 상세페이지 (디자인 추출 대상)
Google Sheets 구조: 7개 시트 완성
도메인: 보유 중 (tworld-store.com)

📚 티월드스토어 웹빌더 - 프로젝트 연속성 문서 v2.0
🔧 Part A: 기술 명세서
A.1 기술 스택 (확정)
Frontend (Admin)
yamlFramework: React 18.2.0
Language: TypeScript 5.0
Build Tool: Vite 5.0
State Management: Zustand 4.4
UI Library: Ant Design 5.12
Page Builder: Craft.js 0.2
Drag & Drop: React DnD 16.0
Editor: TinyMCE 6.0
HTTP Client: Axios 1.6
Backend
yamlRuntime: Node.js 18 LTS
Framework: Firebase Functions
Database: Firestore
Storage: Firebase Storage + Cloudinary
Auth: Firebase Authentication
Hosting: Vercel (Frontend) + Firebase (Admin)
Data Layer
yamlPrimary Source: Google Sheets
Sync Engine: Google Apps Script
Cache: products.json (GitHub)
CDN: Cloudflare
Real-time: Firestore
Development Tools
yamlVersion Control: Git + GitHub
CI/CD: GitHub Actions
Package Manager: npm 9.0
Linter: ESLint
Formatter: Prettier
Testing: Jest + React Testing Library
```

---

### **A.2 시스템 아키텍처**

#### **데이터 플로우**
```
Google Sheets (마스터 데이터)
    ↓ [GAS - 1시간 주기]
products.json (GitHub)
    ↓ [CDN 캐싱]
Frontend API 호출
    ↓
페이지 렌더링

병렬 처리:
Firestore (페이지 설정) → Admin → HTML 생성 → Vercel
API 엔드포인트 구조
javascript// Firebase Functions
/api/
  /products
    GET /list              // 상품 목록
    GET /:id              // 상품 상세
  /pages
    GET /:pageId          // 페이지 데이터
    POST /generate        // HTML 생성
    POST /publish         // 배포
  /images
    POST /upload          // 이미지 업로드
    POST /process         // 이미지 처리
  /forms
    POST /consultation    // 상담 신청
  /sync
    POST /sheets          // 시트 동기화
    POST /telegram        // 텔레그램 알림

A.3 데이터베이스 스키마
Firestore Collections
typescript// pages 컬렉션
interface Page {
  id: string;
  title: string;
  slug: string;
  sections: Section[];
  meta: MetaData;
  status: 'draft' | 'published';
  version: number;
  history: Version[];
  createdAt: Timestamp;
  updatedAt: Timestamp;
  publishedAt?: Timestamp;
}

// sections 구조
interface Section {
  id: string;
  type: SectionType;
  order: number;
  settings: Record<string, any>;
  responsive: {
    desktop: SectionSettings;
    tablet?: SectionSettings;
    mobile?: SectionSettings;
  };
}

// products 컬렉션 (Sheets 동기화)
interface Product {
  id: string;              // 갤럭시S24_256GB
  brand: string;
  model: string;
  storage: number;
  price: number;
  colors: Color[];
  displaySettings: DisplaySettings;
  images: ProductImages;
  syncedAt: Timestamp;
}

// consultations 컬렉션
interface Consultation {
  id: string;
  customerName: string;
  phone: string;
  deviceId: string;
  planId: string;
  message?: string;
  status: 'new' | 'processing' | 'completed';
  createdAt: Timestamp;
  notificationSent: {
    telegram: boolean;
    sms?: boolean;
    kakao?: boolean;
  };
}

A.4 모듈 구조
공유 모듈 (ES6)
javascript// shared/modules/api.js
export class DataAPI {
  constructor(config) {
    this.baseUrl = config.apiUrl;
    this.cache = new Map();
  }
  
  async load() { }
  async getDevice(id) { }
  async getDevicesByBrand(brand) { }
  async getPlans() { }
  async getSubsidy(params) { }
}

// shared/modules/calculator.js
export class PriceCalculator {
  constructor(settings) {
    this.interestRate = settings.할부이자율;
    this.discountRate = settings.선약할인율;
  }
  
  calculate(params) { }
  calculateInstallment(principal, months, rate) { }
  getSubsidyAmount(type, device, plan) { }
}

// shared/modules/validators.js
export class Validators {
  static phone(value) { }
  static email(value) { }
  static required(value) { }
  static minLength(value, min) { }
}

A.5 보안 설정
Firebase Security Rules
javascript// Firestore Rules
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Public read
    match /pages/{page} {
      allow read: if resource.data.status == 'published';
      allow write: if request.auth != null && 
                     request.auth.token.admin == true;
    }
    
    // Products - read only
    match /products/{product} {
      allow read: if true;
      allow write: if false; // Only via Functions
    }
    
    // Consultations - write only for users
    match /consultations/{consultation} {
      allow create: if true;
      allow read, update, delete: if request.auth != null;
    }
  }
}

// Storage Rules
service firebase.storage {
  match /b/{bucket}/o {
    match /images/{allPaths=**} {
      allow read: if true;
      allow write: if request.auth != null;
    }
  }
}

A.6 환경 변수
bash# .env.production
VITE_FIREBASE_API_KEY=
VITE_FIREBASE_AUTH_DOMAIN=
VITE_FIREBASE_PROJECT_ID=
VITE_FIREBASE_STORAGE_BUCKET=

VITE_CLOUDINARY_CLOUD_NAME=
VITE_CLOUDINARY_UPLOAD_PRESET=

VITE_GOOGLE_SHEETS_ID=
VITE_GAS_WEB_APP_URL=

VITE_TELEGRAM_BOT_TOKEN=
VITE_TELEGRAM_CHAT_ID=

VITE_API_BASE_URL=
VITE_CDN_URL=
```

---

## 🎨 **Part B: 기능 명세서**

### **B.1 사용자 기능 (고객용 사이트)**

#### **메인 페이지**
| 기능 | 설명 | 구현 상태 |
|------|------|-----------|
| 히어로 배너 | 슬라이더 3-5개 | Admin에서 관리 |
| 인기 상품 | 자동/수동 선택 | Sheets 기반 |
| 프로모션 | 이벤트 배너 | Admin에서 관리 |
| 빠른 상담 | CTA 버튼 | 상담폼 연결 |

#### **상품 목록**
| 기능 | 설명 | 구현 상태 |
|------|------|-----------|
| 브랜드 필터 | 삼성/애플/기타 | URL 파라미터 |
| 정렬 | 가격/이름/최신순 | 클라이언트 처리 |
| 페이지네이션 | 20개씩 표시 | 무한 스크롤 옵션 |
| 검색 | 모델명 검색 | 실시간 필터링 |

#### **상품 상세**
| 기능 | 설명 | 구현 상태 |
|------|------|-----------|
| 이미지 갤러리 | 색상별 이미지 | Swiper.js |
| 용량 선택 | 128/256/512GB | 버튼 그룹 |
| 색상 선택 | HEX 컬러칩 | 이미지 연동 |
| 가격 계산 | 실시간 계산 | calculator.js |
| 상담 신청 | 하단 고정 CTA | 폼 연결 |

#### **상담 시스템**
| 기능 | 설명 | 구현 상태 |
|------|------|-----------|
| 상담 폼 | 고객정보 입력 | 유효성 검증 |
| 시트 저장 | 자동 저장 | GAS API |
| 텔레그램 알림 | 즉시 전송 | 기존 코드 활용 |
| 신청 확인 | 화면 표시 | 신청번호 생성 |
| SMS/카톡 | 고객 알림 | 추후 구현 |

---

### **B.2 관리자 기능 (Admin)**

#### **대시보드**
| 기능 | 설명 | 우선순위 |
|------|------|----------|
| 통계 위젯 | 방문자, 상담, 매출 | 중 |
| 할일 목록 | 오늘의 작업 | 상 |
| 상담 현황 | 실시간 업데이트 | 상 |
| 시스템 상태 | 동기화 상태 | 중 |

#### **페이지 빌더**
| 기능 | 설명 | 우선순위 |
|------|------|----------|
| 섹션 추가 | 20종 이상 템플릿 | 상 |
| 드래그앤드롭 | 순서 변경 | 상 |
| 실시간 편집 | 인라인 에디터 | 상 |
| 반응형 설정 | PC/태블릿/모바일 | 중 |
| 미리보기 | iframe 실시간 | 상 |
| 버전 관리 | 히스토리 저장 | 중 |

#### **게시판 관리**
| 기능 | 설명 | 우선순위 |
|------|------|----------|
| 게시판 생성 | 무제한 추가 | 상 |
| 템플릿 선택 | 5종 기본 제공 | 상 |
| 권한 설정 | 읽기/쓰기 | 하 |
| 카테고리 | 다단계 지원 | 중 |

#### **이미지 관리**
| 기능 | 설명 | 우선순위 |
|------|------|----------|
| 업로드 | 드래그앤드롭 | 상 |
| 자동 최적화 | WebP 변환 | 상 |
| AI 편집 | 배경제거, 크롭 | 중 |
| 갤러리 | 폴더 관리 | 중 |
| CDN 연동 | 자동 배포 | 상 |

#### **상품 관리**
| 기능 | 설명 | 우선순위 |
|------|------|----------|
| 시트 동기화 | 1시간/수동 | 상 |
| 상품 이미지 | 색상별 관리 | 상 |
| 가격 설정 | 시트에서만 | 상 |
| 재고 표시 | 상태 관리 | 중 |

#### **메뉴 관리**
| 기능 | 설명 | 우선순위 |
|------|------|----------|
| 메뉴 추가 | 무제한 | 상 |
| 순서 변경 | 드래그앤드롭 | 중 |
| 하위 메뉴 | 2단계 지원 | 하 |
| 링크 설정 | 내부/외부 | 상 |

---

### **B.3 운영 프로세스**

#### **일일 운영**
```
시간대별 작업:
09:00 - 텔레그램 확인, 상담 응대
10:00 - 배너 교체, 이벤트 업데이트
14:00 - 상품 정보 업데이트
17:00 - 익일 준비, 긴급 대응
```

#### **주간 운영**
```
요일별 작업:
월: 주간 프로모션 설정
수: 상품 이미지 업데이트  
금: 주말 이벤트 준비
```

#### **월간 운영**
```
정기 작업:
- 데이터 백업
- 성과 분석
- 시스템 점검
- 비용 검토
```

---

### **B.4 데이터 흐름도**

#### **상담 신청 프로세스**
```
고객 입력 → 유효성 검증 → Firestore 저장
                              ↓
                        Google Sheets
                              ↓
                        텔레그램 알림
                              ↓
                        (추후) SMS/카톡
```

#### **상품 업데이트 프로세스**
```
Sheets 수정 → GAS 트리거 → JSON 생성
                              ↓
                          GitHub 업로드
                              ↓
                          CDN 캐시 갱신
                              ↓
                          사이트 반영

B.5 UI/UX 명세
디자인 시스템 (기존 HTML 기반)
css/* 추출된 디자인 토큰 */
--color-primary: #3617CE;
--color-secondary: #FF6B00;
--radius-sm: 4px;
--radius-md: 8px;
--shadow-card: 0 2px 8px rgba(0,0,0,0.1);
```

#### **반응형 브레이크포인트**
```
Mobile: 0-767px
Tablet: 768-1023px  
Desktop: 1024px+
```

#### **페이지 로딩 목표**
```
목표: 2초 이내
이미지: Lazy Loading
스크립트: 비동기 로드
캐싱: Service Worker